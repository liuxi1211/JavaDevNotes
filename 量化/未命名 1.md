● # akquant 能力核查需求
我现在要核查 akquant 的能力，决定如何使用

  ## 核查目标

  确认 akquant 回测引擎在以下三个层面的能力边界，用于决定因子计算架构：
  - 如果 akquant 内置了技术指标计算 →我将全部使用 akquant 内置的计算能力，不写任何指标计算
  - 如果 akquant 不内置 → 我需要自行实现因子计算函数

  ## 需要核查的问题

  ### 1. 技术指标计算能力

  akquant 是否内置了以下技术指标的计算？请逐个确认，并给出调用方式（函数签名 / 类方法）。

  | 指标 | 是否内置 | 调用方式 | 备注 |
  |---|---|---|---|
  | MA（简单移动均线） | ? | | |
  | EMA（指数移动均线） | ? | | |
  | MACD | ? | | |
  | RSI | ? | | |
  | KDJ | ? | | |
  | BOLL（布林带） | ? | | |
  | ATR | ? | | |
  | OBV | ? | | |
  | WR（威廉指标） | ? | | |
  | CCI | ? | | |
  | DMI | ? | | |
  | SAR | ? | | |

  如果内置了部分，请说明是通过什么方式提供的：
  - a) Strategy 基类的方法（如 `self.ma(period=5)`）
  - b) 单独的指标模块（如 `akquant.indicators.ma()`）
  - c) 集成第三方库（如 ta-lib / pandas-ta）
  - d) 其他方式

  ### 2. Strategy 类的 on_bar 上下文

  回测时，Strategy 子类的 `on_bar(self, bar)` 方法中：

  - `bar` 对象包含哪些字段？（close, open, high, low, volume... 还有没有更多？）
  - Strategy 基类是否提供历史数据访问？（如获取前 N 根 bar 的收盘价）
  - Strategy 基类是否提供持仓查询？（如 `self.get_position()`）
  - Strategy 基类是否提供交易方法？（如 `self.buy()`, `self.sell()`, `self.close_position()`）
  - 能否在 on_bar 中直接计算技术指标，还是必须在外部预计算后传入？

  请给出 Strategy 基类的完整接口定义。

  ### 3. 回测 API 调用方式

  `akquant.run_backtest()` 的完整函数签名是什么？参数包括哪些？特别关注：

  - 如何传入行情数据？（DataFrame 格式要求）
  - 如何传入策略类？（类引用 or 实例 or 字符串？）
  - 是否支持自定义佣金 / 滑点 / 印花税？
  - 是否支持多标的同时回测？
  - 是否支持基准对比？
  - 回测结果返回什么格式？（对象 / dict？包含哪些字段？）

  ### 4. 参数优化能力

  `akquant.run_grid_search()` 或类似函数：

  - 是否存在？函数签名是什么？
  - 是否支持并行（n_jobs）？
  - 支持哪些优化目标指标？（sharpe, return, drawdown...）
  - 结果返回格式是什么？

  ### 5. Walk-forward 分析能力

  akquant 是否内置 Walk-forward 分析（滚动窗口回测 + 样本外验证）？

  - 如果有，函数签名和参数是什么？
  - 如果没有，能否基于现有 API 组合实现？

  ### 6. 报告生成能力

  akquant 是否内置回测报告生成？

  - 是否生成 HTML 报告？
  - 用什么图表库？（Plotly / Matplotlib / 其他）
  - 报告包含哪些内容？（权益曲线、回撤、交易记录...）
  - 能否自定义报告？

  ### 7. 信号生成

  akquant 是否支持单独的信号生成模式（不回测，只对最新数据产出买卖信号）？

  - 如果支持，如何调用？
  - 如果不支持，能否用 run_backtest 跑到最新日期来模拟？

  ## 核查输出要求

  1. 对每个问题给出明确结论（有/没有 + 代码证据）
  2. 附上关键代码片段（函数签名、类定义、调用示例）
  3. 如果某个能力不存在，说明是否有替代方案或 workaround
  4. 最后给出一个总结表：akquant 能做什么、不能做什么


